export const SectionLabel = ({ label }: { label: string }) => (
  <div className="section-label mb-10">
    <span className="font-display text-[11px] tracking-[0.14em] uppercase text-(--text-muted) whitespace-nowrap">
      {label}
    </span>
  </div>
)
